# Anecdotes
Exercises from 1.12 to 1.14 are related to part 1 of the course.


## Run locally
From this root folder `./part1/anecdotes`, on the command line run

``` bash
npm install
npm run dev
```
The application is visible in the browser on the default port of http://localhost:5173/